#!/bin/bash

# Скрипт для автоматического переподключения Bluetooth-колонки (Яндекс Станция 2).
# Нужен, потому что при первом автоподключении система выбирает неправильный профиль (HSP/HFP вместо A2DP),
# и звук не выводится. Переподключение через bluetoothctl исправляет это.

DEVICE="B8:87:6E:56:87:3F"

sleep 2

bluetoothctl disconnect $DEVICE

sleep 2

bluetoothctl connect $DEVICE