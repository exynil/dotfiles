#!/bin/bash

# Что делает этот скрипт:
# - Настраивает расположение мониторов в зависимости от hostname

hostname=$(uname -n)

if [ "$hostname" = "arch-rrg" ]; then
    hdmi="HDMI-0"
    dp="DP-4"
    xrandr --output "$dp" --pos 1920x0 --rotate right \
    --output "$hdmi" --mode 1920x1080 --pos 0x420
fi
