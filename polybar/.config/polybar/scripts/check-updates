#!/bin/bash

# # Проверка наличия обновлений и подсчет их количества
# updates=$(yay -Qu 2>/dev/null | wc -l)

# # Если не удалось получить количество обновлений, установить значение 0
# updates="${updates:-0}"

# # Вывод количества обновлений, если они есть
# if [ "$updates" -gt 0 ]; then
#     echo "$updates"
# else
#     echo ""
# fi

#!/bin/bash

# Получаем количество обновлений с помощью yay и подсчитываем их количество
updates=$(yay -Qu 2>/dev/null | wc -l)

# Выводим количество обновлений, если они есть, иначе пустую строку
echo "${updates:-}"
